name: quickmove
description: QuickMove - Ride sharing app for Pakistan
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.5
  flutter_riverpod: ^2.4.9

  # Networking
  http: ^1.1.2
  dio: ^5.4.0
  web_socket_channel: ^2.4.5

  # Local Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^8.0.0

  # Location & Dates
  geolocator: ^9.0.2
  intl: ^0.17.0
  firebase_core: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


